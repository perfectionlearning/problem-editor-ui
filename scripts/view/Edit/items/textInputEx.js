//=======================================================
// Answer: TextInputEx (TextInputView)
//=======================================================

;(function() {
	// Instance of RichEditView
	app.TextInputExView = app.TextInputView.extend({
		header: 'Answer text',
		field: 'a' // how does it work? - this is what the model saves
	});
})();
