//=======================================================
// Hint (RichEditView)
//=======================================================
;(function() {
	// Instance of RichEditView
	app.HintView = app.RichEditView.extend({
		header: 'Hint',
		field: 'hint'
	});
})();
