//=======================================================
// Comments for authors or reviewers (temporary!) -- text input
//=======================================================
;(function() {
	app.CommentView = app.RichEditView.extend({
		header: 'Author comments',
		field: 'comment',
	});
})();
